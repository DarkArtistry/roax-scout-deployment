version: '3.9'

services:
  frontend:
    image: darkartistry/blockscout-frontend:validator
    pull_policy: always
    restart: always
    container_name: 'frontend'
    env_file:
      -  ../envs/common-frontend.env
